import { concreteSubject } from './concreteSubject';
import { listener } from './listener';
export class PageVisibility {
    constructor() {
        this.pageVisibilityFn = 'pageVisibilityFn';
        this.onPageVisiblityChangeFn = (e) => {
            concreteSubject.notifyAll(document.hidden ? 'pageHidden' : 'pageVisible');
        };
    }
    initListener() {
        listener.addListener('visibilitychange', document, this.onPageVisiblityChangeFn);
    }
}
export const pageVisibility = new PageVisibility;
//# sourceMappingURL=pageVisibility.js.map